#include <stdio.h>
int main ()
{
  int array_nums[7], i, x, k;
  printf("Input the first element of the array:\n");
  scanf("%d", &x);
    x=i;
  for (k = 0; k < 7; k++)
  {
    array_nums[i];
    i=i*2;
  }
 
    printf("array_nums[%d] = %d\n", k, array_nums[k]);
  
}
